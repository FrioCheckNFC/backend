# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=nfcproject_dev
DB_PASSWORD=changeme
DB_NAME=nfcproject_db

# JWT Authentication — OBLIGATORIO. Genera uno seguro con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CAMBIA_ESTO_POR_UN_SECRETO_LARGO_Y_ALEATORIO

# Environment
NODE_ENV=development

# FIX #1: CORS — dominios permitidos separados por coma (sin espacios)
# En produccion poner solo los dominios reales del frontend
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4200,http://localhost:5173

# FIX (forgot-password): URL base del frontend para generar el link de reset
FRONTEND_URL=http://localhost:3000

# Docker (opcional)
PGADMIN_EMAIL=admin@friocheck.com
PGADMIN_PASSWORD=changeme
